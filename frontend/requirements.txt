# Frontend Dependencies

# [MediaPipe] - 실시간 자세 추정 및 시각화
@mediapipe/camera_utils (via CDN)
@mediapipe/pose (via CDN)
@mediapipe/drawing_utils (via CDN)

# [Three.js] - 3D 렌더링 및 아바타(VRM) 제어
three == 0.168.0
three/addons/ == 0.168.0
@pixiv/three-vrm == 3.1.2
